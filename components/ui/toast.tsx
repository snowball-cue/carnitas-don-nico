"use client";

// Sonner-based toast — re-export helper for imperative calls.
// For the visual <Toaster /> component, see ./sonner.tsx.
export { toast } from "sonner";
export type { ToasterProps } from "sonner";
